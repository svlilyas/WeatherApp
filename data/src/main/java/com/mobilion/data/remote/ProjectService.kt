package com.mobilion.data.remote

interface ProjectService {
    object Endpoint {
        const val mainPath = "/Weather"

        object Weather {
            const val productsPath = "products"
        }
    }
}
